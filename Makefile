build:
		docker compose build

up:
		docker compose up

down:
		docker compose down

start:
		docker compose start

stop:
		docker compose stop
app-bash:
		docker compose exec app bash

logs:
		docker compose logs -f

